import { NavigationContainer } from '@react-navigation/native';
import { NavPages } from './NavegationPage';

export const Routes = () => {
    return (
        <NavigationContainer>
            <NavPages />
        </NavigationContainer>
    );
}
